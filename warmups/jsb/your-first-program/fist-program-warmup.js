//Task 1
console.log("Hello World!");

//In a browser, the console displays messages in the developer tools (DevTools).
//In Node.js, the console is your terminal/command line.

//Task 2
let num = 42;
let str = "Hello";
let bool = true;

console.log(num, typeof num);   // 42 "number"
console.log(str, typeof str);   // "Hello" "string"
console.log(bool, typeof bool); // true "boolean"

//Task 3
let message = "Hello";
// console.log(mesage); // Error: ReferenceError: mesage is not defined

// The message says that the variable mesage was not found — this suggests a typo.

//alert() displays a pop-up window with a message.

//prompt() opens a window with an input field.

//console.log() outputs information to the console (useful for debugging).

//The console is needed during training to see the results of the code and detect errors. An error message usually indicates the type of error (syntax, ReferenceError, etc.) and its location in the code.

//Value vs Type:

let x = 7;          // value 7
console.log(typeof x); // "number" — type

//That is, a value is a specific number, string, or true/false, and a type is a category of that value.